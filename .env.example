# Copy to .env (do not commit .env)
# These map to IConfiguration keys:
# EmailSettings:AcsConnectionString
# EmailSettings:FromEmail
# EmailSettings:ToEmail

EmailSettings__AcsConnectionString=UseAPlaceholderLikeEndpoint=...;AccessKey=...
EmailSettings__FromEmail=sender@example.com
EmailSettings__ToEmail=recipient@example.com